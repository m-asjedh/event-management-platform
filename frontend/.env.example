# Empty = same origin. Vite proxies /healthz, /me, /events, /sessions, and
# /api/auth so the Better Auth cookie stays on localhost:5173.
#
# Set a full URL only if the UI talks to the API without the proxy (CORS
# would then have to allow this origin; auth already trusts
# http://localhost:5173).
VITE_API_BASE=
